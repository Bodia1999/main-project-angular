import { RevertTimePipe } from './revert-time.pipe';

describe('RevertTimePipe', () => {
  it('create an instance', () => {
    const pipe = new RevertTimePipe();
    expect(pipe).toBeTruthy();
  });
});
