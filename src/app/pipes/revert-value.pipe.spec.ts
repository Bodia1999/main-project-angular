import { RevertValuePipe } from './revert-value.pipe';

describe('RevertValuePipe', () => {
  it('create an instance', () => {
    const pipe = new RevertValuePipe();
    expect(pipe).toBeTruthy();
  });
});
